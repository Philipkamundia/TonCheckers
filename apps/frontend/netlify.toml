[build]
  base    = "apps/frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA routing — serve index.html for all routes
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
