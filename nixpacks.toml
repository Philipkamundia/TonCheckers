[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "npm install --ignore-scripts",
  "npm rebuild --workspace=apps/backend"
]

[phases.build]
cmds = ["npm run build --workspace=apps/backend"]

[start]
cmd = "node apps/backend/dist/src/index.js"
